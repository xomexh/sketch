{
  "name": "@sketch/shared",
  "version": "0.12.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown src/index.ts --format esm --out-dir dist --dts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "tsdown": "^0.14.0"
  }
}
