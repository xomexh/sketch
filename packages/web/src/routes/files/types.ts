/**
 * Shared types for the Files route module.
 * API types (ConnectorConfig, UnifiedFile, etc.) come from @/lib/api.
 */

export type { ConnectorConfig, ConnectorFile, FileAccess, FileContent, SearchResult, UnifiedFile } from "@/lib/api";
export type { IntegrationDefinition, IntegrationType } from "@/lib/integrations";
